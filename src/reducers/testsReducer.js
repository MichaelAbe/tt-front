
const initialState = {
    tests: [],
    loading: true
}



const testReducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default testReducer